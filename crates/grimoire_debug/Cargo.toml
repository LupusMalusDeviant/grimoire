[package]
name = "grimoire_debug"
description = "Versioned debug IPC protocol for tooling, profiler and stats overlay."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license-file.workspace = true
publish.workspace = true

[features]
# TCP transport (contract §13); force-enabled workspace-wide by `grimoire_link` later.
tcp = []
# Trait conformance suite (contract §2 rule 12); off by default, no third-party dependency.
conformance = []

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
# Parses the profiler's JSON export in tests (already pinned in the workspace, used the same way by
# grimoire_bench and grimoire_sigilc); never a normal dependency of this crate.
serde_json.workspace = true

[lints]
workspace = true
